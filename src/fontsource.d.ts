// Fontsource packages ship CSS with no TypeScript declarations. These are
// side-effect imports (in +layout.svelte) that register the @font-face rules
// for the families referenced by --font-sans / --font-display.
declare module '@fontsource-variable/inter';
declare module '@fontsource-variable/space-grotesk';
